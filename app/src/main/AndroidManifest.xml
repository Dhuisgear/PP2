<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student.pp2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Rejestracja" />
        <activity
            android:name=".ProfileActivity"
            android:label="Moje konto" />
        <activity
            android:name=".SettingsActivity"
            android:label="Ustawienia" />
        <activity
            android:name=".AllRepairsActivity"
            android:label="Wszystkie naprawy" />
        <activity
            android:name=".CreateRepairActivity"
            android:label="Dodanie naprawy" />
        <activity
            android:name=".EditRepairActivity"
            android:label="Edycja/usunięcie naprawy" />
        <activity android:name=".UserRepairsActivity"></activity>
    </application>

</manifest>